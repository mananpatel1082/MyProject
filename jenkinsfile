pipeline {
    agent { label 'mylinuxagent' }

    stages {
        stage('Check Version') {
            steps {
                'node --version'
                'npm --version'
            }
        }
    }
}
