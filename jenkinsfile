pipeline {
    agent { label 'mylinuxagent' }

    stages {
        stage('Check Version') {
            steps {
                sh 'node --version'
                sh 'npm --version'
            }
        }
    }
}
